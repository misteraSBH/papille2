{% extends "base.html.twig" %}

{% block title %}Restaurant index{% endblock %}

{% block body %}
    <h1>Restaurant index</h1>

    <table class="table">
        <tr>
            <th>Order Id</th>
            <th>Order</th>
            <th></th>
        </tr>

        {% for orderSlip in orderSlips %}
        <tr>
            <td>

                        <p>OrderSlip {{ orderSlip.id }} - Order {{ orderSlip.purchase.id }}</p>
                        <ul>
                            {%  for purchaseItem in orderSlip.purchaseItems %}
                                <li>
                                    {{ purchaseItem.quantity }} {{ purchaseItem.name }} {{ purchaseItem.price }}€
                                </li>
                            {% endfor %}
                        </ul>


                </td>
            </tr>
        {% endfor %}


    </table>
{% endblock %}