{% extends "base.html.twig" %}

{% block body %}
    <h1>Restaurant {{ restaurant.name }} - show dishes</h1>

    <table class="table">
        {% for dish in restaurant.dishes %}
            <tr>
                <td>{{ dish.name }}</td>
                <td>{{ dish.price }} €</td>

            </tr>
        {% endfor %}
    </table>
{% endblock %}