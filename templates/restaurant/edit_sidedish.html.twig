{%  extends 'base.html.twig' %}

{% block title %}Edit d'un Sidedish {% endblock %}
{% block body %}
    <h1>Edit Sidedish</h1>
    {{ form_start(form,{'attr' : {novalidate: 'novalidate'}}) }}
        {{ form_widget(form) }}
    <button type="submit" class="btn btn-primary">Edit</button>
    {{ form_end(form) }}

{% endblock %}