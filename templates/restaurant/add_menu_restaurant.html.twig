{%  extends 'base.html.twig' %}

{% block title %}Add Menu Restaurant {% endblock %}
{% block body %}
    {{ form_start(form,{'attr' : {novalidate: 'novalidate'}}) }}
        {{ form_widget(form) }}
        <button type="submit" class="btn btn-primary">Add</button>
    {{ form_end(form) }}

{% endblock %}