{% extends "base.html.twig" %}

{% block body %}
    <h1>Restaurant {{ restaurant.name }} - show beverages</h1>

    <table class="table">
        {% for beverage in restaurant.beverages %}
            <tr>
                <td>{{ beverage.name }}</td>
                <td>{{ beverage.price }} €</td>
            </tr>
        {% endfor %}
    </table>

    <a href="#" class="btn btn-primary">Add Beverage</a>
{% endblock %}