{%  extends 'base.html.twig' %}

{% block title %}Edit d'un Beverage {% endblock %}
{% block body %}
    <h1>Edit Beverage</h1>
    {{ form_start(form,{'attr' : {novalidate: 'novalidate'}}) }}
        {{ form_widget(form) }}
        <button type="submit" class="btn btn-primary">Editer</button>
    {{ form_end(form) }}

{% endblock %}